#include <stdlib.h>
#include <stdio.h>
#include <time.h>

// aaaaaaaaaaaa tnc desse karaio

int main(){
	int x, y;

	printf("Insira o tamanho do vetor X: ");
	scanf(" %i",& x);

	printf("Insira o tamanho do vetor Y: ");
	scanf(" %i",& y);

	int vx[x], vy[y], aux;

	srand(time(NULL));
}